ssh -T git@github.com
git add .
git commit -m "message"
git push origin main --force

tqdm bool is named like this --> show_{function_name}_progress

git ls-files | grep '.ipynb_checkpoints'
git ls-files | grep '__pycache__'
